import * as React from "react";

const Login = () => {
  return (
    <div>
      Login Page Working!!!!
      {/* {console.log({ props })}
      <LoginContainer history={props.history} /> */}
    </div>
  );
};

export default Login;
